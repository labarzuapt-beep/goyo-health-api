{
  "functions": {
    "api/index.js": {
      "maxDuration": 10
    }
  }
}
```

(Changed `places.js` to `index.js`)

**4. Commit the change**

---

**OR even simpler - just delete the vercel.json file entirely:**

**1. Click on `vercel.json`**

**2. Click trash icon (delete)**

**3. Commit**

We don't actually need it for a basic function.

---

**After doing one of these, wait for Vercel to redeploy and try again:**
```
https://[your-url].vercel.app/api?lat=47.5&lng=-122
